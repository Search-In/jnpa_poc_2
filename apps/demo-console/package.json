{
  "name": "@jnpa/demo-console",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Presenter control console (Addendum B) — drives the same event backbone as live connectors, offline + deterministic. Shared across UC1/2/3 via the registry.",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@esri/calcite-components": "^3.3.0",
    "@esri/calcite-components-react": "^3.3.0",
    "@jnpa/schemas": "workspace:*",
    "@jnpa/sim": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^5.4.11"
  }
}
